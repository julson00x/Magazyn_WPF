<UserControl x:Class="Magazyn_WPF.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Orientation="Vertical" Margin="20">
            <TextBlock Text="Lista Produktów" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <DataGrid x:Name="ProductDataGrid" AutoGenerateColumns="False" Margin="0,0,0,20" 
                      CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}" Width="150"/>
                    <DataGridTextColumn Header="Kategoria" Binding="{Binding Kategoria}" Width="120"/>
                    <DataGridTextColumn Header="Ilość" Binding="{Binding Ilość}" Width="80"/>
                    <DataGridTextColumn Header="Jednostka" Binding="{Binding Jednostka}" Width="100"/>
                    <DataGridTextColumn Header="Lokalizacja" Binding="{Binding Lokalizacja}" Width="140"/>
                    <DataGridTextColumn Header="Data Dodania" Binding="{Binding DataDodania, StringFormat=dd/MM/yyyy}" Width="120" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Dodaj" Padding="15,10" Margin="0,0,10,0" Background="#4CAF50" Foreground="White" FontSize="14"/>
                <Button Content="Edytuj" Padding="15,10" Margin="0,0,10,0" Background="#2196F3" Foreground="White" FontSize="14"/>
                <Button Content="Usuń" Padding="15,10" Margin="0,0,10,0" Background="#F44336" Foreground="White" FontSize="14"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
